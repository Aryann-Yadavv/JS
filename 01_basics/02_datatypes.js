"use strict"; //treats all JS code as newer version of Js

// alert("hello"); we're using node JS, not browser, alert can't be used here

let name = "aryan"
let age = 21
let isLoggedin = false


//let keyword can define all boolean, string, numerical
/* number = has a range of 1 to 2 to power 53, 
bigInt > number, big int is bigger than number keyword,
boolean = true/false, 
string = " " , putt a string in it 
null = standalone value, 
undefined = value not defined yet, 
symbol = used in REACT for uniquness*/


console.log(typeof age) // typeof is used to know the type of the input


console.log(typeof undefined) // undefined
console.log(typeof null) //null is an object as well
